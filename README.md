# 不動産キャッシュフロー計算

スマートフォンで使いやすい、依存関係のない不動産投資シミュレーターです。元利均等返済によるローン返済額・残高、運用中のキャッシュフロー、売却後の最終収支をリアルタイムで計算します。

## 起動

`index.html` をブラウザで直接開くか、ローカルサーバーを起動します。

```sh
python3 -m http.server 8000
```

その後 `http://localhost:8000` を開いてください。

## テスト

```sh
node test.js
```
